// Налаштування сторінки: вимикаємо нумерацію для титулки
#set page(numbering: none)

// Базові налаштування тексту (шрифт, розмір, міжрядковий інтервал)
#set text(font: "Merriweather", size: 12pt, lang: "uk")
#set par(leading: 0.65em)

// --- ВЕРХНІЙ БЛОК (ШАПКА) ---
#align(center)[
  *НАЦІОНАЛЬНИЙ ТЕХНІЧНИЙ УНІВЕРСИТЕТ УКРАЇНИ \
  «КИЇВСЬКИЙ ПОЛІТЕХНІЧНИЙ ІНСТИТУТ \
  імені Ігоря СІКОРСЬКОГО» \
  Навчально-науковий фізико-технічний інститут* \
  
  #v(1em)
  
  *Кафедра математичних методів захисту інформації*
]

// Аналог \vspace{40mm}
#v(40mm)

// --- ЦЕНТРАЛЬНИЙ БЛОК (НАЗВА) ---
#align(center)[
  #text(size: 24pt)[*Звіт до лабораторної № 1*] \
  #text(size: 18pt)[*за темою:*] \
  #text(size: 18pt)[*Провести налаштування Litecoin та виконати тестові операції в системі*]
]

// Аналог \vspace{55mm}
#v(90mm)

// --- ПРАВИЙ БЛОК (АВТОР) ---
// Аналог \hfill та minipage з flushright
#align(right)[
  #block(width: 60%, align(right)[
    *Виконав:* \ 
     ФІ-52мн Ісаченко Н. C.
  ])
]

// Аналог \vfill (заповнює весь вільний простір до кінця сторінки)
#v(1fr)

// --- НИЖНІЙ БЛОК (ДАТА ТА МІСТО) ---
#align(center)[
  м. Київ \
  2026р.
]